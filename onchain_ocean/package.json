{
  "name": "onchain-ocean-root",
  "version": "1.0.0",
  "description": "Root workspace proxy configuration for Onchain Ocean",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix app",
    "build": "npm run build --prefix app",
    "preview": "npm run preview --prefix app",
    "lint": "npm run lint --prefix app"
  }
}
